# Dylint builds this cdylib with `dylint-link` as the linker, which appends the
# toolchain channel to the output filename (lib<name>@<toolchain>.so) so the
# dylint driver can locate and load it.
[target.'cfg(all())']
linker = "dylint-link"
