{
  "name": "@neherlab/app-desktop",
  "version": "0.0.1",
  "private": true,
  "description": "TreeTime desktop application - Electron shell",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "bun --bun oxlint src/ renderer/",
    "lint:fix": "bun --bun oxlint --fix src/ renderer/",
    "format": "bun --bun oxfmt --write src/ renderer/",
    "format:check": "bun --bun oxfmt --check src/ renderer/",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.renderer.json --noEmit && tsc -p tsconfig.node.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@neherlab/app-contracts": "workspace:*",
    "@neherlab/app-napi": "workspace:*",
    "@neherlab/app-ui": "workspace:*",
    "@tailwindcss/vite": "4.3.0",
    "react": "catalog:",
    "react-dom": "catalog:",
    "tailwindcss": "4.3.0"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "6.0.2",
    "electron": "42.2.0",
    "typescript": "catalog:",
    "vite": "8.0.14",
    "vite-plugin-electron": "0.29.1",
    "vitest": "catalog:"
  },
  "build": {
    "appId": "org.neherlab.treetime",
    "productName": "TreeTime",
    "directories": {
      "output": "../../.build/desktop/release"
    },
    "files": [
      "dist-electron/**/*",
      "dist/**/*"
    ],
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ]
    },
    "mac": {
      "target": [
        "dmg"
      ]
    },
    "win": {
      "target": [
        "nsis"
      ]
    }
  }
}
