use crate::graph::edge::GraphEdge;
use crate::graph::graph::Graph;
use crate::graph::node::GraphNode;
use log::warn;

pub fn ladderize<N: GraphNode, E: GraphEdge>(graph: &Graph<N, E>) {
  warn!("not implemented: ladderize");
}
