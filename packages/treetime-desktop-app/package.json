{
  "name": "@neherlab/treetime-desktop-app",
  "version": "0.0.1",
  "private": true,
  "description": "TreeTime desktop application - Electron shell",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && electron .",
    "dev": "tsc && electron .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@neherlab/treetime-contracts": "workspace:*",
    "electron": "^35.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
