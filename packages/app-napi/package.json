{
  "name": "@neherlab/app-napi",
  "version": "0.0.1",
  "private": true,
  "description": "TreeTime desktop application - Rust native addon for Electron",
  "main": "index.js",
  "napi": {
    "binaryName": "app-napi",
    "targets": [
      "x86_64-unknown-linux-gnu",
      "x86_64-apple-darwin",
      "aarch64-apple-darwin"
    ]
  },
  "scripts": {
    "build": "CARGO_TERM_QUIET=true napi build --platform --release --target-dir=../../.build/docker --no-js --no-dts-header",
    "build:debug": "CARGO_TERM_QUIET=true napi build --platform --target-dir=../../.build/docker --no-js --no-dts-header"
  },
  "devDependencies": {
    "@napi-rs/cli": "3.6.2"
  }
}
